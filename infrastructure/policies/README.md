# Runtime policy boundary

The Lambda role may read only the two named AWS Secrets Manager resources and invoke only the approved Titan embedding and Nova guidance models. Browser input never supplies SQL; Managed MCP receives application-owned, allowlisted query templates.
